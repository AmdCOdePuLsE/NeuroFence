# PostgreSQL Database Configuration
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/neurofence_hack
DB_HOST=localhost
DB_PORT=5432
DB_NAME=neurofence_hack
DB_USER=postgres
DB_PASSWORD=postgres

# API Server Configuration
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=true
LOG_LEVEL=INFO

# NeuroFence Configuration
EMBEDDING_MODEL=all-MiniLM-L6-v2
EMBEDDING_DIMENSION=384
CONTAMINATION_THRESHOLD=0.70
ISOLATION_ENABLED=true

# Note: NeuroFence itself does not require any LLM API keys.
# Keep secrets in a local `.env` file (gitignored) if your own application needs them.
